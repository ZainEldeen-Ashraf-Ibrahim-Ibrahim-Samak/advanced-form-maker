# Quickstart

No new architectural dependencies are introduced that require a complex setup process, other than installing the sanitization package.

1. Install sanitization library (if not already present):
   `npm install isomorphic-dompurify`
2. Run standard dev setup:
   `npm run dev`
